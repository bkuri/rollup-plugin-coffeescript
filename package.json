{
  "author": "Bernardo Kuri",
  "description": "Modern CoffeeScript support for Rollup.",
  "homepage": "https://github.com/bkuri/rollup-plugin-coffeescript",
  "license": "MIT",
  "module": "dist/rollup-plugin-coffeescript.mjs",
  "name": "@rollup/plugin-coffeescript",
  "type": "module",
  "version": "0.2.0",
  "bugs": {
    "url": "https://github.com/bkuri/rollup-plugin-coffeescript/issues"
  },
  "dependencies": {
    "coffeescript": "^2.7.0"
  },
  "devDependencies": {
    "@rollup/pluginutils": "^5.0.2",
    "del-cli": "^5.0.0",
    "mocha": "^10.1.0",
    "rollup": "^3.2.3"
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "keywords": [
    "coffeescript",
    "rollup",
    "plugin",
    "npm",
    "module"
  ],
  "peerDependencies": {
    "coffeescript": "2.7.0",
    "rollup": "^3.2.3"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "url": "bkuri/rollup-plugin-coffeescript"
  },
  "scripts": {
    "build": "rollup -c",
    "ci:coverage": "nyc pnpm test && nyc report --reporter=text-lcov > coverage.lcov",
    "ci:lint": "pnpm build && pnpm lint",
    "ci:lint:commits": "commitlint --from=${CIRCLE_BRANCH} --to=${CIRCLE_SHA1}",
    "ci:test": "pnpm test -- --verbose",
    "prebuild": "del-cli dist",
    "prepare": "if [ ! -d 'dist' ]; then pnpm build; fi",
    "prerelease": "pnpm build",
    "pretest": "pnpm build",
    "release": "pnpm plugin:release --workspace-root -- --pkg $npm_package_name",
    "test": "mocha"
  }
}
